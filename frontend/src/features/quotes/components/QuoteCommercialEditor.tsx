import { useState } from "react";
import { Button } from "../../../shared/components/ui/Button";
import { FormField } from "../../../shared/components/ui/FormField";
import type { CommercialDetailsInput, QuoteCommercial } from "../types/quote.types";
export function QuoteCommercialEditor({ commercial, isSaving, onSave }: { commercial: QuoteCommercial; isSaving: boolean; onSave: (input: CommercialDetailsInput) => Promise<void> }) { const [validUntil, setValidUntil] = useState(commercial.validUntil ?? ""); const [paymentTerms, setPaymentTerms] = useState(commercial.paymentTerms ?? ""); const [commercialNotes, setCommercialNotes] = useState(commercial.commercialNotes ?? ""); const [internalNotes, setInternalNotes] = useState(commercial.internalNotes ?? ""); return <section className="quote-panel quote-editor"><h2>Condiciones comerciales</h2><div className="quote-editor-fields"><FormField label="Vigencia" htmlFor="quote-valid-until"><input className="form-control" id="quote-valid-until" type="date" value={validUntil} onChange={(event) => setValidUntil(event.target.value)} disabled={isSaving} /></FormField><FormField label="Condiciones de pago" htmlFor="quote-payment-terms"><input className="form-control" id="quote-payment-terms" value={paymentTerms} onChange={(event) => setPaymentTerms(event.target.value)} disabled={isSaving} /></FormField></div><FormField label="Observaciones comerciales" htmlFor="quote-commercial-notes"><textarea className="form-control" id="quote-commercial-notes" value={commercialNotes} onChange={(event) => setCommercialNotes(event.target.value)} disabled={isSaving} /></FormField><FormField label="Notas internas" htmlFor="quote-internal-notes"><textarea className="form-control" id="quote-internal-notes" value={internalNotes} onChange={(event) => setInternalNotes(event.target.value)} disabled={isSaving} /></FormField><Button type="button" isLoading={isSaving} onClick={() => onSave({ currency: "PEN", validUntil: validUntil || null, paymentTerms: paymentTerms || null, commercialNotes: commercialNotes || null, internalNotes: internalNotes || null })}>Guardar condiciones</Button></section>; }
